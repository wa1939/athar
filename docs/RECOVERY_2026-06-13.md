# Recovery session — 2026-06-13

This note records the recovered Codex session after the power loss and the follow-up integration work, so future work does not repeat or fork the same vision.

## Recovered goal

Continue Athar as a best-in-class, local-first TMOAP replacement:

- Understand the existing app, master brief, roadmap, and the TMOAP Excel workflow before adding features.
- Reduce manual transaction entry through reliable SMS, notification, CSV, recurring, categorization, and local learning flows.
- Make the app useful beyond one user's data: more banks, safer parsing, fewer false positives, better diagnostics, and scalable user-defined templates.
- Keep strict branch discipline: scoped dev branches, PRs to `main`, no disruption to the working app, and no release-readiness claim without tests and documentation.
- Preserve the privacy model: no backend, no telemetry, no cloud categorization without explicit user action.

Recovered local artifacts:

- Full transcript: `C:\tmp\athar-recovered-session-2026-06-13.md`
- Handoff summary: `C:\tmp\athar-recovered-handoff-2026-06-13.md`
- Integration audit: `C:\tmp\athar-pr-stack-integration-audit-2026-06-13.txt`

## Integrated stack

Local integration branch: `dev/integration-recovered-stack`.

Merged recovered branches, in order:

1. `dev/sms-corpus-reliability`
2. `dev/bulk-categorize-stable-import`
3. `dev/queued-sms-dispatcher`
4. `dev/widget-pending-actions-refresh`
5. `dev/manual-entry-quick-add`
6. `dev/plan-goals`
7. `dev/bills-calendar`
8. `dev/notification-ingestion-coverage`
9. `dev/flexible-statement-csv-import`
10. `dev/tax-export-pdf`
11. `dev/wishlist-priority-capacity`
12. `dev/support-diagnostics-export`
13. `dev/local-category-rule-learning`
14. `dev/ingestion-account-routing`
15. `dev/recurring-auto-run`
16. `dev/onboarding-first-import`
17. `dev/user-template-preview`
18. `dev/sms-audit-sender-health`
19. `dev/history-source-filter`
20. `dev/rescan-confirmation`

Integration conflicts were resolved additively:

- Plan keeps both Bills and Goals tabs.
- Ingestion keeps queued dispatch plus generic notification parsing.
- Settings keeps both tax/accountant PDF export and redacted support diagnostics.
- App startup keeps Hilt WorkManager configuration and recurring auto-run scheduling.

## Validation

Use JDK 17. System Java 26 is not compatible with this Gradle/Kotlin setup.

Final passing command:

```powershell
$env:JAVA_HOME='C:\Users\waok\.codex\jdks\jdk-17.0.19+10'
$env:PATH="$env:JAVA_HOME\bin;$env:PATH"
$env:ANDROID_HOME='C:\Users\waok\Android\Sdk'
.\gradlew.bat --console=plain test :app:assemblePersonalFullSmsDebug :app:assembleStoreSafeDebug :app:lintPersonalFullSmsDebug :app:lintStoreSafeDebug
```

Result: `BUILD SUCCESSFUL`.

Additional integration repairs:

- Settings unit-test fakes now cover both tax export and support diagnostics export.
- `personalFullSms` declares `android.hardware.telephony` as optional so SMS permissions do not force telephony-only hardware in lint.

## Runtime QA status

Device/emulator E2E and screenshot-based UI audit are not complete yet.

Current blocker on this PC:

- `adb devices -l` shows no connected Android device.
- AVD `AtharPixelQaApi35` exists.
- `emulator -accel-check` reports that the Android Emulator hypervisor driver is not installed.
- Earlier software launches of `AtharPixelQaApi35` stayed offline or exited before exposing ADB.
- The W-09 `dev/wishlist-planning-summary` retry on 2026-06-14 crashed before install/launch: headless `-accel off` launches exited with Windows access-violation code `-1073741819`; the windowed-hidden retry logged `Failed to load opengl32sw` and opened an emulator crash dialog while `adb devices -l` stayed empty.
- The G-11 `dev/notification-balance-amount-selection` retry on 2026-06-14 still could not install or launch: a bounded hidden `AtharPixelQaApi35` software boot left only `emulator-5554 offline` in ADB and no emulator process.
- The G-11 `dev/notification-marketing-discount-guards` retry on 2026-06-14 still could not install or launch: after clearing a stale `qemu-system-x86_64-headless` process and AVD lock files, a bounded hidden `AtharPixelQaApi35` software boot stayed `emulator-5554 offline` in ADB for 3 minutes.
- The G-11 `dev/notification-reward-amount-guards` retry on 2026-06-14 still could not install or launch: a bounded hidden `AtharPixelQaApi35` software boot stayed `emulator-5554 offline` in ADB for 3 minutes, then the stuck headless qemu process and lock files were cleaned up.
- The G-11 `dev/notification-security-code-amount-guards` retry on 2026-06-14 still could not install or launch: a bounded hidden `AtharPixelQaApi35` software boot emitted startup logs but never exposed an online ADB device; stale AVD lock files were cleaned afterward.
- The G-11 `dev/notification-authorization-hold-guards` retry on 2026-06-14 still could not install or launch: a bounded hidden `AtharPixelQaApi35` software boot logged `Failed to load opengl32sw`, showed an emulator crash dialog, never exposed an online ADB device, and left AVD locks that were cleaned afterward.
- The G-11 `dev/notification-balance-only-hint-guards` retry on 2026-06-14 still could not install or launch: a bounded hidden `AtharPixelQaApi35` software boot logged `Failed to load opengl32sw`, showed an emulator crash dialog, never exposed an online ADB device, and left AVD locks that were cleaned afterward.
- The G-11 `dev/notification-credit-state-guards` retry on 2026-06-14 still could not install or launch: a bounded hidden `AtharPixelQaApi35` software boot logged `Failed to load opengl32sw`, showed an emulator crash dialog, never exposed an online ADB device, and left AVD locks that were cleaned afterward.
- The G-11 `dev/notification-admin-notice-guards` retry on 2026-06-14 still could not install or launch: a bounded hidden `AtharPixelQaApi35` software boot logged `Failed to load opengl32sw`, showed an emulator crash dialog, never exposed an online ADB device, and left AVD locks that were cleaned afterward.
- The G-11 `dev/notification-spending-summary-guards` retry on 2026-06-14 still could not install or launch: a bounded hidden `AtharPixelQaApi35` software boot logged `Failed to load opengl32sw`, showed an emulator crash dialog, never exposed an online ADB device, and left AVD locks that were cleaned afterward.
- The R-14 `dev/private-corpus-micro-parser-audit` retry on 2026-06-14 still could not install or launch: a bounded hidden `AtharPixelQaApi35` software boot logged `Failed to load opengl32sw`, showed an emulator crash dialog, never exposed an online ADB device, and left AVD locks that were cleaned afterward.
- The R-05 `dev/curated-seed-rules-global-common-batch` retry on 2026-06-14 still could not install or launch: a bounded hidden `AtharPixelQaApi35` boot with `-accel off` emitted startup logs for about three minutes but never exposed an online ADB device; the default x86_64 launch then exited with `x86_64 emulation currently requires hardware acceleration`, and a stale `multiinstance.lock` was cleaned afterward.
- The R-15 `dev/private-corpus-admin-ignore-followup` retry on 2026-06-14 still could not install or launch: a bounded hidden `AtharPixelQaApi35` boot with `-accel off` emitted startup logs but never exposed an online ADB device. Cleanup completed with no attached ADB device, no emulator/qemu process, and no AVD lock files.
- The R-16 `dev/alrajhi-electronic-payment-labels` retry on 2026-06-14 still could not install or launch: a bounded hidden `AtharPixelQaApi35` boot with `-accel off` emitted startup logs but never exposed an online ADB device. Cleanup completed with no attached ADB device, no emulator/qemu process, and no AVD lock files.
- The R-05 `dev/curated-seed-rules-post-r16-audit` retry on 2026-06-14 still could not install or launch: a bounded hidden `AtharPixelQaApi35` boot with `-accel off` emitted startup logs but stayed `emulator-5554 offline` in ADB. The stuck headless qemu process and AVD lock files were cleaned afterward.
- The S-21 `dev/support-diagnostics-category-backlog` retry on 2026-06-14 still could not install or launch: the built `personalFullSmsDebug` APK was ready, but two bounded hidden `AtharPixelQaApi35` software launches (`-gpu swiftshader_indirect` and `-gpu off`, each with `-accel off`) exited before exposing ADB with Windows access-violation code `-1073741819`. AVD lock files were cleaned afterward; final cleanup had no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The R-07 `dev/category-backlog-action-summary` retry on 2026-06-14 still could not install or launch: the built `personalFullSmsDebug` APK was ready, but a bounded hidden `AtharPixelQaApi35` software launch (`-no-window -no-snapshot -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) exited after 5 seconds before exposing ADB with Windows access-violation code `-1073741819`. AVD lock files were cleaned afterward; final cleanup had no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The R-07 `dev/bulk-categorize-category-options` retry on 2026-06-14 still could not install or launch: the x86_64 `AtharPixelQaApi35` software launch (`-no-window -no-snapshot -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) stayed `emulator-5554 offline` for the bounded wait, and `AtharPixelQaApi35Arm` exited because arm64 system images are not supported by this x86_64 host. Cleanup finished with no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The R-17 `dev/private-audit-missing-merchant-shapes` retry on 2026-06-14 still could not install or launch: the x86_64 `AtharPixelQaApi35` software launch (`-no-window -no-snapshot -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) stayed `emulator-5554 offline` for the bounded wait. The launched emulator and headless qemu processes were stopped, stale AVD locks were removed, and cleanup finished with no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The R-18 `dev/private-audit-uncategorized-groups` retry on 2026-06-14 still could not install or launch: the x86_64 `AtharPixelQaApi35` software launch (`-no-window -no-snapshot -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) stayed `emulator-5554 offline` for the bounded wait. The launched emulator and headless qemu processes were stopped, stale AVD locks were removed, and cleanup finished with no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The R-07 `dev/bulk-categorize-group-import` retry on 2026-06-14 still could not install or launch: the built `personalFullSmsDebug` APK was ready, but the x86_64 `AtharPixelQaApi35` software launch (`-no-window -no-snapshot -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) exited before exposing ADB with Windows access-violation code `-1073741819`. Stale `hardware-qemu.ini.lock` and `multiinstance.lock` files were removed, and cleanup finished with no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The R-07 `dev/bulk-categorize-exact-import-rules` retry on 2026-06-14 still could not install or launch: the built `personalFullSmsDebug` APK was ready, but the x86_64 `AtharPixelQaApi35` software launch (`-no-window -no-snapshot -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) briefly exposed only `emulator-5554 offline`, then exited before install with Windows access-violation code `-1073741819`. Stale `hardware-qemu.ini.lock` and `multiinstance.lock` files were removed, and cleanup finished with no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The R-07 `dev/bulk-categorize-type-safe-import` retry on 2026-06-14 still could not install or launch: the built `personalFullSmsDebug` APK was ready, and `emulator -accel-check` reported that the Android Emulator hypervisor driver is not installed. A bounded hidden x86_64 `AtharPixelQaApi35` software launch (`-no-window -no-snapshot -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) reached only `emulator-5554 offline`, so install/screenshot/logcat capture could not run. Stuck qemu/ADB processes were stopped, AVD locks were removed, and final cleanup had no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The R-07 `dev/bulk-categorize-skip-breakdown` retry on 2026-06-14 still could not install or launch: the built `personalFullSmsDebug` APK was ready, and `emulator -accel-check` reported that the Android Emulator hypervisor driver is not installed. A bounded hidden x86_64 `AtharPixelQaApi35` software launch (`-no-window -no-snapshot-load -no-snapshot-save -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) stayed `emulator-5554 offline`, so install/screenshot/UI-dump/logcat capture could not run. Runtime evidence was written to `build/qa/bulk-categorize-skip-breakdown-emulator/`; the emulator/qemu processes were stopped, stale AVD lock files were removed, and final cleanup had no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The R-07 `dev/bulk-categorize-copy-prompt` retry on 2026-06-14 still could not install or launch: the built `personalFullSmsDebug` APK was ready, and `emulator -accel-check` reported that the Android Emulator hypervisor driver is not installed. A bounded hidden x86_64 `AtharPixelQaApi35` software launch (`-no-window -no-snapshot-load -no-snapshot-save -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) stayed `emulator-5554 offline`, so install/screenshot/UI-dump/logcat capture could not run. Runtime evidence was written to `build/qa/bulk-categorize-copy-prompt-emulator/`; the emulator/qemu processes were stopped, stale AVD lock files were removed, and final cleanup had no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The R-07 `dev/bulk-categorize-fenced-csv-import` retry on 2026-06-14 still could not install or launch: the built `personalFullSmsDebug` APK was ready, and `emulator -accel-check` reported that the Android Emulator hypervisor driver is not installed. A bounded hidden x86_64 `AtharPixelQaApi35` software launch (`-no-window -no-snapshot-load -no-snapshot-save -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) exited before exposing an online ADB device with Windows access-violation code `-1073741819`, so install/screenshot/UI-dump/logcat capture could not run. Runtime evidence was written to `build/qa/bulk-categorize-fenced-csv-import-emulator/`, stale AVD lock files were removed, and final cleanup had no emulator/qemu/netsim process and no AVD lock files.
- The R-07 `dev/bulk-categorize-private-export` retry on 2026-06-14 still could not install or launch: the built `personalFullSmsDebug` APK was ready, and `emulator -accel-check` reported that the Android Emulator hypervisor driver is not installed. A bounded hidden x86_64 `AtharPixelQaApi35` software launch (`-no-window -no-snapshot-load -no-snapshot-save -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) exited before exposing an online ADB device with Windows access-violation code `-1073741819`, so install/screenshot/UI-dump/logcat capture could not run. Runtime evidence was written to `build/qa/bulk-categorize-private-export-emulator/`, stale AVD lock files were removed, and final cleanup had no emulator/qemu/netsim process and no AVD lock files.
- The G-11 `dev/notification-trailing-party-hints` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, but the x86_64 `AtharPixelQaApi35` software launch (`-no-window -no-snapshot -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) briefly exposed only `emulator-5554 offline`, then exited before install with Windows access-violation code `-1073741819`. Stale `hardware-qemu.ini.lock` and `multiinstance.lock` files were removed, ADB was restarted, and cleanup finished with no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The G-11 `dev/notification-regional-currency-symbols` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, but the x86_64 `AtharPixelQaApi35` software launch (`-no-window -no-snapshot -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) exited before exposing ADB with Windows access-violation code `-1073741819`. Stale `hardware-qemu.ini.lock` and `multiinstance.lock` files were removed, ADB was restarted, and cleanup finished with no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The G-11 `dev/notification-balance-after-extraction` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, but the x86_64 `AtharPixelQaApi35` software launch (`-no-window -no-snapshot -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) exited after 2 seconds before exposing an online ADB device with Windows access-violation code `-1073741819`. Stale `hardware-qemu.ini.lock` and `multiinstance.lock` files were removed, ADB was restarted, and cleanup finished with no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The R-05 `dev/curated-seed-rules-private-followup` retry on 2026-06-14 still could not install or launch: the built `personalFullSmsDebug` APK was ready, and `emulator -accel-check` still reported that the Android Emulator hypervisor driver is not installed. Two bounded hidden `AtharPixelQaApi35` software launches (`-gpu swiftshader_indirect -accel off` and `-gpu off -accel off`) emitted startup logs but never exposed an online ADB device. Cleanup finished with no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The G-11 `dev/notification-refund-reversal-income` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, and `emulator -accel-check` still reported that the Android Emulator hypervisor driver is not installed. Two bounded hidden `AtharPixelQaApi35` software launches (`-gpu swiftshader_indirect -accel off` and `-gpu off -accel off`) exited before exposing an online ADB device with Windows access-violation code `-1073741819`. Stale `hardware-qemu.ini.lock` and `multiinstance.lock` entries were removed, and cleanup finished with no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The G-11 `dev/notification-atm-withdrawal-guards` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, and `emulator -accel-check` still reported that the Android Emulator hypervisor driver is not installed. A bounded hidden `AtharPixelQaApi35` software launch (`-gpu swiftshader_indirect -accel off`) stayed `emulator-5554 offline` in ADB for 3 minutes. The stale `qemu-system-x86_64-headless` process and AVD lock files were cleaned afterward; final cleanup had no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The G-11 `dev/notification-bank-fee-guards` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, and `emulator -accel-check` still reported that the Android Emulator hypervisor driver is not installed. A bounded hidden `AtharPixelQaApi35` software launch (`-gpu swiftshader_indirect -accel off`) exited before exposing an online ADB device with Windows access-violation code `-1073741819`. Stale AVD lock files were cleaned afterward; final cleanup had no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The G-11 `dev/notification-debt-payment-labels` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, and `emulator -accel-check` still reported that the Android Emulator hypervisor driver is not installed. A bounded hidden `AtharPixelQaApi35` software launch (`-gpu swiftshader_indirect -accel off`) stayed `emulator-5554 offline` in ADB for 185 seconds. The new emulator/qemu processes and stale AVD lock files were cleaned afterward; final cleanup had no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The G-11 `dev/notification-utility-bill-labels` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready. A bounded hidden `AtharPixelQaApi35` software launch (`-gpu swiftshader_indirect -accel off`) stayed `emulator-5554 offline` in ADB until the boot window timed out, so install/screenshot/logcat capture could not run. The stuck emulator/qemu/netsim processes and stale AVD lock files were cleaned afterward; final cleanup had no emulator/qemu/netsim process and no AVD lock files.
- The G-11 `dev/notification-salary-income-labels` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, and `emulator -accel-check` still reported that the Android Emulator hypervisor driver is not installed. A bounded hidden `AtharPixelQaApi35` software launch (`-gpu swiftshader_indirect -accel off`) exposed only `emulator-5554 offline`, so install/screenshot/logcat capture could not run. The stuck emulator/qemu/netsim processes and stale AVD lock files were cleaned afterward; final cleanup had no emulator/qemu/netsim process and no AVD lock files.
- The G-11 `dev/notification-recurring-expense-labels` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, and `emulator -accel-check` still reported that the Android Emulator hypervisor driver is not installed. A bounded hidden `AtharPixelQaApi35` software launch (`-gpu swiftshader_indirect -accel off`) stayed `emulator-5554 offline` for the logged wait, so install/screenshot/logcat capture could not run. Runtime evidence was written to `build/qa/notification-recurring-expense-emulator/`, and cleanup finished with no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The G-11 `dev/notification-telecom-recharge-labels` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, and `emulator -accel-check` still reported that the Android Emulator hypervisor driver is not installed. A bounded hidden `AtharPixelQaApi35` software launch (`-gpu swiftshader_indirect -accel off`) stayed `emulator-5554 offline` for the logged wait, so install/screenshot/logcat capture could not run. Runtime evidence was written to `build/qa/notification-telecom-recharge-emulator/`, and cleanup finished with no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The G-11 `dev/notification-public-service-labels` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, and `emulator -accel-check` still reported that the Android Emulator hypervisor driver is not installed. A bounded hidden `AtharPixelQaApi35` software launch (`-gpu swiftshader_indirect -accel off`) exited with Windows access-violation code `-1073741819` before exposing an online ADB device, so install/screenshot/logcat capture could not run. Runtime evidence was written to `build/qa/notification-public-service-emulator/`, stale AVD locks were cleaned, and final cleanup had no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The G-11 `dev/notification-mobility-payment-labels` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, and `emulator -accel-check` still reported that the Android Emulator hypervisor driver is not installed. A bounded hidden `AtharPixelQaApi35` software launch (`-gpu swiftshader_indirect -accel off`) exited with Windows access-violation code `-1073741819` before exposing an online ADB device, so install/screenshot/logcat capture could not run. Runtime evidence was written to `build/qa/notification-mobility-payment-emulator/`; stale AVD locks were cleaned afterward, and final cleanup had no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The G-11 `dev/notification-pos-transaction-phrases` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, and `emulator -accel-check` reported that the Android Emulator hypervisor driver is not installed. A bounded hidden `AtharPixelQaApi35` software launch (`-no-window -no-snapshot-load -no-snapshot-save -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) stayed `emulator-5554 offline` during the wait, so install/screenshot/UI-dump/logcat capture could not run. Runtime evidence was written to `build/qa/notification-pos-transaction-phrases-emulator/`, and final cleanup had no attached ADB device, no adb/emulator/qemu/netsim process, and no AVD lock files.
- The G-11 `dev/notification-location-labels` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, and `emulator -accel-check` reported that the Android Emulator hypervisor driver is not installed. A bounded hidden `AtharPixelQaApi35` software launch (`-no-window -no-snapshot-load -no-snapshot-save -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) stayed `emulator-5554 offline` during the wait, so install/screenshot/UI-dump/logcat capture could not run. Runtime evidence was written to `build/qa/notification-location-labels-emulator/`, and final cleanup had no attached ADB device, no adb/emulator/qemu/netsim process, and no AVD lock files.
- The G-11 `dev/notification-payment-request-guards` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, and `emulator -accel-check` reported that the Android Emulator hypervisor driver is not installed. A bounded hidden `AtharPixelQaApi35` software launch (`-no-window -no-snapshot-load -no-snapshot-save -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) stayed `emulator-5554 offline` during the wait, so install/screenshot/UI-dump/logcat capture could not run. Runtime evidence was written to `build/qa/notification-payment-request-guards-emulator/`, and final cleanup had no attached ADB device, no adb/emulator/qemu/netsim process, and no AVD lock files.
- The G-11 `dev/notification-status-prefix-merchant-hints` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, and `emulator -accel-check` reported that the Android Emulator hypervisor driver is not installed. A bounded hidden `AtharPixelQaApi35` software launch (`-no-window -no-snapshot-load -no-snapshot-save -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) stayed `emulator-5554 offline` during the wait, so install/screenshot/UI-dump/logcat capture could not run. Runtime evidence was written to `build/qa/notification-status-prefix-merchant-hints-emulator/`; the new emulator/qemu processes and stale AVD locks were cleaned, and post-inspection cleanup had no attached ADB device, no adb/emulator/qemu/netsim process, and no AVD lock files.
- The G-11 `dev/notification-recipient-label-hints` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, and `emulator -accel-check` reported that the Android Emulator hypervisor driver is not installed. A bounded hidden `AtharPixelQaApi35` software launch (`-no-window -no-snapshot-load -no-snapshot-save -no-audio -no-boot-anim -gpu swiftshader_indirect -accel off`) stayed `emulator-5554 offline` during the wait, so install/screenshot/UI-dump/logcat capture could not run. Runtime evidence was written to `build/qa/notification-recipient-label-hints-emulator/`; the new emulator/qemu processes and stale AVD locks were cleaned, and post-inspection cleanup verification had no attached ADB device, no adb/emulator/qemu/netsim process, and no AVD lock files.
- The G-11 `dev/notification-card-used-merchant-hints` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, and `emulator -accel-check` reported that the Android Emulator hypervisor driver is not installed. `AtharPixelQaApi35Arm` exited because arm64 system images are not supported on this x86_64 host; a visible x86 `AtharPixelQaApi35` software launch opened an emulator crash dialog and left stale locks; after stopping the orphaned qemu/crashpad helpers and clearing only those AVD locks, a clean hidden x86 software launch (`-no-window -no-snapshot-load -no-snapshot-save -no-audio -no-boot-anim -gpu off -accel off`) stayed `emulator-5554 offline` through the bounded wait. Install/screenshot/UI-dump/logcat capture could not run. Runtime evidence was written under `build/qa/notification-card-used-merchant-hints-emulator*`, and cleanup verification had no adb/emulator/qemu/netsim process and no AVD lock files.
- The G-11 `dev/notification-card-used-at-merchant-hints` retry on 2026-06-14 still could not install or launch: the built `storeSafeDebug` APK was ready, and `emulator -accel-check` reported that the Android Emulator hypervisor driver is not installed. A bounded hidden `AtharPixelQaApi35` software launch (`-no-window -no-snapshot-load -no-snapshot-save -no-audio -no-boot-anim -gpu off -accel off`) emitted startup logs and exited before an online ADB device appeared, so install/screenshot/UI-dump/logcat capture could not run. Runtime evidence was written to `build/qa/notification-card-used-at-merchant-hints-emulator/`; stale AVD locks were cleaned, and cleanup verification had no adb/emulator/qemu/netsim process and no AVD lock files.
- The R-05 `dev/seed-rules-private-name-hygiene` branch on 2026-06-14 removes 51 legacy private-person or ambiguous local side-income patterns from the public seed, adds an asset guard requiring low-priority side-income seeds to be public or institution-backed, and brings active `seed_rules.json` to 667 rules. `:core:data:testDebugUnitTest`, the forced gated private SMS audit, `git diff --check`, and the full JVM test/build/lint stack passed with JDK 21. The forced gated private SMS audit wrote only redacted aggregate output: 7,887 records, 3,864 parser successes, 0 parser failures, 0 failed known-bank messages, 2,374 parsed expenses, 1,276 categorized parsed expenses, 1,098 uncategorized parsed expenses, 0 missing-merchant parsed expenses, 0 inactive public-catalog matches, and `rawBodiesWritten = 0`. Runtime `personalFullSmsDebug` install was attempted; `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` software launch did not expose a boot-complete online ADB device. Evidence was written to `build/qa/seed-rules-private-name-hygiene-emulator/`, and final cleanup had no attached ADB device, no qemu/emulator/netsim process, and no AVD lock files.
- The R-07 `dev/bulk-categorize-audit-raw-context` branch on 2026-06-14 makes full-context bulk-categorization CSV export fill `raw_body` from linked local SMS/notification audit bodies when available, while private export keeps `raw_body` blank. `:core:data:testDebugUnitTest`, the forced gated private SMS audit, and the full JVM test/build/lint stack passed with JDK 21. The forced gated private SMS audit wrote redacted aggregate output only: 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, and 0 missing-merchant parsed expenses. Runtime `personalFullSmsDebug` install was attempted; `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` software launch exited before exposing ADB with Windows access-violation code `-1073741819`. Evidence was written to `build/qa/bulk-categorize-audit-raw-context-emulator/`, and final cleanup had no attached ADB device, no emulator/qemu/netsim process, and no AVD locks.
- The R-07 `dev/bulk-categorize-exact-rule-upsert` branch on 2026-06-14 makes bulk categorization imports treat exact learned merchant rules as upserts: repeated imports do not duplicate the same exact local rule, corrected imports replace stale exact local rules, and seed/system exact rules plus explicit substring rules are preserved. `:core:data:testDebugUnitTest`, the forced gated private SMS audit, and the full JVM test/build/lint stack passed with JDK 21. The forced gated private SMS audit wrote redacted aggregate output only: 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, and 0 missing-merchant parsed expenses. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, but `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed. A bounded hidden x86_64 `AtharPixelQaApi35` software launch stayed `emulator-5554 offline`, and a follow-up automatic-acceleration launch exited with `x86_64 emulation currently requires hardware acceleration`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/bulk-categorize-exact-rule-upsert-emulator/`, and final cleanup had no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The R-07 `dev/bulk-categorize-category-value-normalization` branch on 2026-06-14 makes bulk categorization imports accept copied category option strings, copied bilingual display labels, and case-variant category IDs while still resolving only active categories, enforcing transaction-type compatibility, preserving skip counters, and training exact local rules only after an unambiguous compatible decision. `:core:data:testDebugUnitTest`, `:core:domain:test`, the forced gated private SMS audit, `git diff --check`, and the full JVM test/build/lint stack passed with JDK 21. The forced gated private SMS audit wrote redacted aggregate output only: 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, 2,374 parsed expenses, 1,276 categorized expenses, 1,098 uncategorized expenses, 0 missing-merchant parsed expenses, and `rawBodiesWritten = 0`. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, but `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed and a bounded hidden x86_64 `AtharPixelQaApi35` software launch stayed `emulator-5554 offline`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/bulk-categorize-category-value-normalization-emulator/`, and cleanup finished with no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The R-18 `dev/private-audit-root-report` branch on 2026-06-14 pins the gated private SMS audit output to repository-root `build/private-corpus-audit/latest.json` even when Gradle runs the parser test from the module working directory. The focused `PrivateSmsExportAuditTest`, forced gated private SMS audit, `git diff --check`, and full JVM test/build/lint stack passed with JDK 21. The root report wrote rich redacted aggregate output only: 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, 2,374 parsed expenses, 1,276 categorized expenses, 1,098 uncategorized expenses, 0 missing-merchant parsed expenses, 20 hashed uncategorized merchant groups, and `rawBodiesWritten = 0`; redaction spot-checks found no raw-body smoke-test strings. Runtime `personalFullSmsDebug` install was attempted with the built APK; `AtharPixelQaApi35` exited before exposing ADB because x86_64 emulation requires hardware acceleration and the Android Emulator hypervisor driver is not installed, while `AtharPixelQaApi35Arm` exited because arm64 system images are unsupported on this x86_64 host. Evidence was written to `build/qa/private-audit-root-report-emulator/`, and cleanup removed the stale AVD lock after confirming no emulator/qemu process was running.
- The G-11 `dev/notification-package-coverage-global-followup` branch on 2026-06-14 adds verified Canadian, German, and Saudi wallet/bank package IDs to both store-safe notification gates and replaces broad ANB substring matching with exact verified ANB package IDs. `:ingestion:notification-listener:test`, `:ingestion:sms-parser:test`, the focused `GenericBankNotificationTemplateTest`, the forced gated private SMS audit, `git diff --check`, and the full JVM test/build/lint stack passed with JDK 21. The forced gated private SMS audit wrote redacted aggregate output only: 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, and 0 missing-merchant parsed expenses. Runtime `storeSafeDebug` install was attempted; the built APK was ready, but `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed and a bounded hidden x86_64 `AtharPixelQaApi35` software launch stayed `emulator-5554 offline`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/notification-package-coverage-global-followup-emulator/`. A follow-up retry also wrote `build/qa/notification-package-coverage-global-followup-emulator-retry/`; `AtharPixelQaApi35Arm` exited because arm64 system images are not supported on this x86_64 host, and the x86_64 AVD emitted startup logs but exited before any online ADB device appeared. Final cleanup had no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The G-11 `dev/notification-essential-life-labels` branch on 2026-06-14 normalizes posted medical, pharmacy, education, charity, and zakat notification payments to exact seed-backed labels, preserves specific clinics/charities when present, ignores essential-life due reminders or donation appeals with amounts, and brings active `seed_rules.json` to 672 rules. The focused `GenericBankNotificationTemplateTest`, `:ingestion:sms-parser:test`, `:ingestion:notification-listener:test`, `:core:data:testDebugUnitTest`, the forced gated private SMS audit, and the full JVM test/build/lint stack passed with JDK 21. The forced gated private SMS audit wrote redacted aggregate output only: 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, and 0 missing-merchant parsed expenses. Runtime `storeSafeDebug` install was attempted; the built APK was ready, but `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed and a bounded hidden x86_64 `AtharPixelQaApi35` software launch stayed `emulator-5554 offline`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/notification-essential-life-labels-emulator/`, and cleanup stopped the qemu/netsim process with no attached ADB device remaining.
- The G-11 `dev/notification-everyday-commerce-labels` branch on 2026-06-14 normalizes posted fuel, grocery, restaurant, coffee, food-delivery, and taxi-ride notification payments to exact seed-backed labels, preserves specific stations/ride merchants when present, ignores everyday-commerce offers, estimates, reservations, and reminders with amounts, keeps Arabic `خصم` debit wallet copy parseable, and brings active `seed_rules.json` to 678 rules. The focused `GenericBankNotificationTemplateTest`, `:ingestion:sms-parser:test`, `:ingestion:notification-listener:test`, `:core:data:testDebugUnitTest`, the forced gated private SMS audit, `git diff --check`, and the full JVM test/build/lint stack passed with JDK 21. The forced gated private SMS audit wrote redacted aggregate output only: 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, and 0 missing-merchant parsed expenses. Runtime `storeSafeDebug` install was attempted; the built APK was ready, but `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed and a bounded hidden x86_64 `AtharPixelQaApi35` software launch exposed only `emulator-5554 offline`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/notification-everyday-commerce-labels-emulator/`, and cleanup left no attached ADB device and no emulator/qemu/netsim process.
- The G-11 `dev/notification-travel-labels` branch on 2026-06-14 normalizes posted flight-ticket, hotel, travel-booking, and car-rental notification payments to exact seed-backed travel labels, preserves named airlines/providers when present, ignores travel offers, itineraries, reservations, quotes, and reminders with amounts, and brings active `seed_rules.json` to 682 rules. The focused `GenericBankNotificationTemplateTest`, `:ingestion:sms-parser:test`, `:ingestion:notification-listener:test`, `:core:data:testDebugUnitTest`, the forced gated private SMS audit, and the full JVM test/build/lint stack passed with JDK 21. The forced gated private SMS audit wrote redacted aggregate output only: 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, and 0 missing-merchant parsed expenses. Runtime `storeSafeDebug` install was attempted; the built APK was ready, but `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed and a bounded hidden x86_64 `AtharPixelQaApi35` software launch did not expose an online ADB device, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/notification-travel-labels-emulator/`, and cleanup left no attached ADB device and no emulator/qemu/netsim process.
- The G-11 `dev/notification-retail-shopping-labels` branch on 2026-06-14 normalizes posted clothing, electronics, and online-shopping notification payments to exact seed-backed retail labels, preserves named stores when present, ignores retail offers, carts, price drops, shipping/order-status copy with amounts, and brings active `seed_rules.json` to 685 rules. The focused `GenericBankNotificationTemplateTest`, `:ingestion:sms-parser:test`, `:ingestion:notification-listener:test`, `:core:data:testDebugUnitTest`, the forced gated private SMS audit, `git diff --check`, and the full JVM test/build/lint stack passed with JDK 21. The forced gated private SMS audit wrote redacted aggregate output only: 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, and 0 missing-merchant parsed expenses. Runtime `storeSafeDebug` install was attempted; the built APK was ready, but `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed and a bounded hidden x86_64 `AtharPixelQaApi35` software launch did not expose a boot-complete online ADB device, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/notification-retail-shopping-labels-emulator/`, and cleanup left no attached ADB device and no emulator/qemu/netsim process.
- The G-11 `dev/notification-entertainment-labels` branch on 2026-06-14 normalizes posted cinema-ticket, event-ticket, and game-purchase notification payments to exact seed-backed entertainment labels, preserves named venues/events when present, ignores entertainment promos, showtimes, presales, reservations, and game offers with amounts, and brings active `seed_rules.json` to 688 rules. The focused `GenericBankNotificationTemplateTest`, `:ingestion:sms-parser:test`, `:ingestion:notification-listener:test`, `:core:data:testDebugUnitTest`, the forced gated private SMS audit, `git diff --check`, and the full JVM test/build/lint stack passed with JDK 21. The forced gated private SMS audit wrote redacted aggregate output only: 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, and 0 missing-merchant parsed expenses. Runtime `storeSafeDebug` install was attempted; the built APK was ready, but `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed and a bounded hidden `AtharPixelQaApi35` software launch did not expose a boot-complete online ADB device, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/notification-entertainment-labels-emulator/`, and cleanup finished with no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The G-11 `dev/notification-life-admin-labels` branch on 2026-06-14 normalizes posted home-service, gym-membership, and childcare/daycare notification payments to exact seed-backed labels, preserves named providers when present, ignores offers, quotes, and reminders with amounts, and brings active `seed_rules.json` to 691 rules. The focused `GenericBankNotificationTemplateTest`, `:ingestion:sms-parser:test`, `:ingestion:notification-listener:test`, `:core:data:testDebugUnitTest`, the forced gated private SMS audit, and the full JVM test/build/lint stack passed with JDK 21. The forced gated private SMS audit wrote redacted aggregate output only: 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, 2,374 parsed expenses, 1,276 categorized expenses, 1,098 uncategorized expenses, 0 missing-merchant parsed expenses, and `rawBodiesWritten = 0`. Runtime `storeSafeDebug` install was attempted; the built APK was ready, but `AtharPixelQaApi35` exited before exposing ADB because x86_64 emulation requires hardware acceleration and the Android Emulator hypervisor driver is not installed, while `AtharPixelQaApi35Arm` exited because arm64 system images are unsupported on this x86_64 host. Evidence was written to `build/qa/notification-life-admin-labels-emulator/`.
- The G-11 `dev/notification-car-maintenance-labels` branch on 2026-06-14 normalizes posted car-service, oil-change, car-wash, and tire-service notification payments to exact seed-backed car-maintenance labels, preserves named providers when present, ignores quotes, estimates, appointments, reminders, and offers with amounts, and brings active `seed_rules.json` to 695 rules. The focused `GenericBankNotificationTemplateTest`, `:ingestion:sms-parser:test`, `:ingestion:notification-listener:test`, `:core:data:testDebugUnitTest`, the forced gated private SMS audit, `git diff --check`, and the full JVM test/build/lint stack passed with JDK 21. The forced gated private SMS audit wrote redacted aggregate output only: 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, 2,374 parsed expenses, 1,276 categorized expenses, 1,098 uncategorized expenses, 0 missing-merchant parsed expenses, and `rawBodiesWritten = 0`. Runtime `storeSafeDebug` install was attempted; the built APK was ready, but `AtharPixelQaApi35` exited before exposing ADB because x86_64 emulation requires hardware acceleration and the Android Emulator hypervisor driver is not installed, while `AtharPixelQaApi35Arm` exited because arm64 system images are unsupported on this x86_64 host. Evidence was written to `build/qa/notification-car-maintenance-labels-emulator/`.
- The G-11 `dev/notification-car-payment-labels` branch on 2026-06-14 normalizes posted auto-loan, vehicle-finance, and Arabic car-instalment notification payments to the exact seed-backed `Car payment` label, preserves generic loan repayments as `Loan instalment`, ignores auto-finance offers, due reminders, schedules, and estimates with amounts, and brings active `seed_rules.json` to 696 rules. The focused `GenericBankNotificationTemplateTest`, `:ingestion:sms-parser:test`, `:ingestion:notification-listener:test`, `:core:data:testDebugUnitTest`, the forced gated private SMS audit, and the full JVM test/build/lint stack passed with JDK 21. The forced gated private SMS audit wrote redacted aggregate output only: 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, 2,374 parsed expenses, 1,276 categorized expenses, 1,098 uncategorized expenses, 0 missing-merchant parsed expenses, and `rawBodiesWritten = 0`. Runtime `storeSafeDebug` install was attempted; the built APK was ready, but `AtharPixelQaApi35` exited before exposing ADB because x86_64 emulation requires hardware acceleration and the Android Emulator hypervisor driver is not installed, while `AtharPixelQaApi35Arm` exited because arm64 system images are unsupported on this x86_64 host. Evidence was written to `build/qa/notification-car-payment-labels-emulator/`.
- The G-11 `dev/notification-mortgage-payment-labels` branch on 2026-06-14 normalizes posted mortgage, home-loan, housing-finance, and Arabic mortgage-instalment notification payments to the exact seed-backed `Mortgage payment` label, preserves generic loan repayments as `Loan instalment`, ignores mortgage offers, due reminders, schedules, refinance/rate estimates, pre-approvals, and eligibility copy with amounts, and brings active `seed_rules.json` to 697 rules. The focused `GenericBankNotificationTemplateTest`, `:ingestion:sms-parser:test`, `:ingestion:notification-listener:test`, `:core:data:testDebugUnitTest`, the forced gated private SMS audit, and the full JVM test/build/lint stack passed with JDK 21. The forced gated private SMS audit wrote redacted aggregate output only: 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, 2,374 parsed expenses, 1,276 categorized expenses, 1,098 uncategorized expenses, 0 missing-merchant parsed expenses, and `rawBodiesWritten = 0`. Runtime `storeSafeDebug` install was attempted; the built APK was ready, but `AtharPixelQaApi35` exited before exposing ADB because x86_64 emulation requires hardware acceleration and the Android Emulator hypervisor driver is not installed, while `AtharPixelQaApi35Arm` exited because arm64 system images are unsupported on this x86_64 host. Evidence was written to `build/qa/notification-mortgage-payment-labels-emulator/`.
- The G-11 `dev/notification-gift-payment-labels` branch on 2026-06-14 normalizes posted gift-card, gift, and flower-delivery notification payments to exact seed-backed gift labels, preserves named florists such as Floward, ignores free-gift promos, gift-card balances, expiry/reminder copy, and offers with amounts, and brings active `seed_rules.json` to 700 rules. The focused `GenericBankNotificationTemplateTest`, `:ingestion:sms-parser:test`, `:ingestion:notification-listener:test`, `:core:data:testDebugUnitTest`, the forced gated private SMS audit, and the full JVM test/build/lint stack passed with JDK 21. The forced gated private SMS audit wrote redacted aggregate output only: 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, 2,374 parsed expenses, 1,276 categorized expenses, 1,098 uncategorized expenses, 0 missing-merchant parsed expenses, 20 hashed uncategorized merchant groups, and `rawBodiesWritten = 0`. Runtime `storeSafeDebug` install was attempted; the built APK was ready, but `AtharPixelQaApi35` exited before exposing ADB because x86_64 emulation requires hardware acceleration and the Android Emulator hypervisor driver is not installed, while `AtharPixelQaApi35Arm` exited because arm64 system images are unsupported on this x86_64 host. Evidence was written to `build/qa/notification-gift-payment-labels-emulator/`.
- The G-11 `dev/notification-home-goods-labels` branch on 2026-06-14 normalizes posted furniture, home-goods, and appliance purchase notification payments to exact seed-backed home/electronics labels, preserves named stores such as IKEA, ignores quotes, carts/wishlists, delivery/order-status, installation/assembly, warranty, reminder, and offer copy with amounts, and brings active `seed_rules.json` to 703 rules. The focused `GenericBankNotificationTemplateTest`, `:ingestion:sms-parser:test`, `:ingestion:notification-listener:test`, `:core:data:testDebugUnitTest`, the forced gated private SMS audit, and the full JVM test/build/lint stack passed with JDK 21. The forced gated private SMS audit wrote redacted aggregate output only: 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, 2,374 parsed expenses, 1,276 categorized expenses, 1,098 uncategorized expenses, 0 missing-merchant parsed expenses, 20 hashed uncategorized merchant groups, and `rawBodiesWritten = 0`. Runtime `storeSafeDebug` install was attempted; the built APK was ready, but `AtharPixelQaApi35` exited before exposing ADB because x86_64 emulation requires hardware acceleration and the Android Emulator hypervisor driver is not installed, while `AtharPixelQaApi35Arm` exited because arm64 system images are unsupported on this x86_64 host. Evidence was written to `build/qa/notification-home-goods-labels-emulator/`.
- The F-10 `dev/category-seed-missing-categories` branch on 2026-06-14 makes bundled category seeding upgrade-safe: app startup inserts only missing bundled category IDs, appends them after the user's current sort order, and preserves edited names, archived state, monthly targets, custom categories, and existing order. It also enables JUnit Platform for Android library unit tests, which exposed and fixed previously silent core-data failures in statement CSV header normalization, XLSX XML padding, support diagnostics decimal redaction, and the side-income seed guard. Focused core-data tests, full `test`, and the full debug APK build/lint stack passed with JDK 21. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` software launch stayed `emulator-5554 offline` for 120 seconds, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/category-seed-missing-categories-emulator/`, and final cleanup restarted ADB with no attached devices and no emulator/qemu/netsim process.
- The G-12 `dev/tmoap-category-parity` branch on 2026-06-14 adds the missing concrete TMOAP workbook category setup defaults (`Condo fees`, `Work`, `Tax refund`, `Expense reimbursement`, `Bonus`, and `Other income`) and makes statement category lookup transaction-type aware, including TMOAP aliases for `Public transportation`, `Wife`, `Job`, `Side project`, and income `Other`. Focused CSV/category asset/prompt tests, full `test`, and the full debug APK build/lint stack passed with JDK 21. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and two bounded hidden `AtharPixelQaApi35` software launches (`-gpu swiftshader_indirect -accel off` and `-gpu off -accel off`) exited after 4 seconds with Windows access-violation code `-1073741819`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/tmoap-category-parity-emulator/` and `build/qa/tmoap-category-parity-emulator-gpu-off/`, and final cleanup left no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The G-12 `dev/tmoap-budget-target-import` branch on 2026-06-14 adds a separate Settings preview-confirm flow for the TMOAP workbook `Budget Targets` sheet. The importer reads category labels from column B and monthly targets from column G, keeps statement import transaction-only, ignores blank target cells, resolves expense and income labels with the shared TMOAP-aware aliases, previews target/change/skipped counts and totals, and updates only matched active categories after confirmation. Focused budget-target/XLSX/Settings tests, `git diff --check`, full `test`, and the full debug APK build/lint stack passed with JDK 21. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` software launch exposed only `emulator-5554 offline`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/tmoap-budget-target-import-emulator/`; cleanup stopped the orphaned qemu process, removed the stale AVD lock, and left no attached ADB device, no emulator/qemu/netsim process, and no AVD lock files.
- The G-12 `dev/tmoap-wishlist-import` branch on 2026-06-14 adds a separate Settings preview-confirm flow for the TMOAP workbook `Wishlist` sheet. The importer reads item, cost, current saved, desired months, and start month rows, accepts Excel serial and text start months, skips invalid rows in preview, creates new wishes, and updates matching existing wishes by normalized item name without duplicating them. Focused Wishlist/Budget Target/Settings tests, `git diff --check`, full `test`, and the full debug APK build/lint stack passed with JDK 21. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` software launch exposed only `emulator-5554 offline`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/tmoap-wishlist-import-emulator/`; cleanup stopped qemu/netsim, removed stale AVD locks, and left no attached ADB device, no qemu/emulator/netsim/adb process, and no AVD lock files.
- The G-12 `dev/tmoap-investments-import` branch on 2026-06-14 adds a separate Settings preview-confirm flow for the TMOAP workbook `استثمارات العائلة` / Family Investments sheet. The importer reads the raw amount/owner contribution table, captures pool return and period, stops at the first blank gap so the lower summary table is not imported, avoids double-counting total return rows, creates a new pool when none exists, and replaces matched pool contributors after confirmation so repeat imports do not duplicate rows. Focused Investment/Wishlist/Settings tests, `git diff --check`, full `test`, and the full debug APK build/lint stack passed with JDK 21. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` software launch exposed only `emulator-5554 offline`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/tmoap-investments-import-emulator/`; cleanup stopped qemu/netsim, removed stale AVD locks, and left no attached ADB device, no qemu/emulator/netsim/adb process, and no AVD lock files.
- The R-18 `dev/private-audit-coverage-summary` branch on 2026-06-14 adds redacted category-coverage and uncategorized-group concentration metrics to the gated private SMS audit. The focused `PrivateSmsExportAuditTest`, `:ingestion:sms-parser:test`, the forced gated private SMS audit, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. The forced root report wrote aggregate output only: 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, 2,374 parsed expenses, 1,287 categorized expenses, 1,087 uncategorized expenses, 0 missing-merchant parsed expenses, top-20 hashed uncategorized groups covering 184 rows, 903 uncategorized rows outside those groups, and `rawBodiesWritten = 0`. Runtime `personalFullSmsDebug` install was attempted; `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed and a bounded hidden `AtharPixelQaApi35` software launch exposed only `emulator-5554 offline`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/private-audit-coverage-summary-emulator/`, and final cleanup left no emulator/qemu/adb/netsim process and no AVD lock entries.
- The H-06 `dev/history-bulk-category-cleanup` branch on 2026-06-14 adds a History selection mode for visible rows, a type-safe bulk category picker, compatible expense/income row confirmation, transfer skips, and applied/skipped feedback without training new category rules. Focused `HistoryFilterTest`/`HistoryViewModelTest`, full `:feature:today:testDebugUnitTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` software launch did not expose a boot-complete online ADB device, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/history-bulk-category-cleanup-emulator/`, and final cleanup left no emulator/qemu/adb/netsim process and no AVD lock files.
- The H-07 `dev/history-select-visible-cleanup` branch on 2026-06-14 adds a `Select shown` action to History selection mode so the current filtered result set can be selected in one action before the existing type-safe category assignment. Focused `HistoryFilterTest`/`HistoryViewModelTest`, full `:feature:today:testDebugUnitTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` software launch stayed `emulator-5554 offline`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/history-select-visible-cleanup-emulator/`; cleanup stopped the orphaned qemu process and left no emulator/qemu/adb/netsim process and no AVD lock files.
- The H-08 `dev/history-select-same-merchant` branch on 2026-06-14 adds a `Same merchant` action to History selection mode so a selected representative visible row can select all currently shown rows sharing its normalized merchant before the existing type-safe category assignment. Focused `HistoryFilterTest`/`HistoryViewModelTest`, full `:feature:today:testDebugUnitTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` software launch did not reach an online ADB device or `boot_completed=1`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/history-select-same-merchant-emulator/`; cleanup removed the stale AVD lock directory and left no emulator/qemu/adb/netsim process and no AVD lock files.
- The H-09 `dev/history-exact-rule-learning` branch on 2026-06-15 makes repeated same-merchant History bulk category assignment create one exact local category rule after compatible selected rows are applied, while mixed merchants, single rows, generic merchants, and explicit non-exact user rules avoid broad learning. The bulk result toast now reports when future same-merchant rows will auto-categorize. Focused `HistoryFilterTest`/`HistoryViewModelTest`, full `:feature:today:testDebugUnitTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` software launch did not reach an online ADB device or `boot_completed=1`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/history-exact-rule-learning-emulator/`; cleanup left no emulator/qemu/adb/netsim process and no AVD lock files.
- The H-10 `dev/history-repeated-backlog-filter` branch on 2026-06-15 adds a localized `Repeated backlog` category-state chip to History so users can isolate actionable repeated uncategorized merchant groups after the current status/type/source/search filters. The filter includes only uncategorized non-transfer rows, groups by normalized merchant plus expense/income kind, ignores generic merchant keys, and reuses the existing selection/category/exact-rule-learning flow. Focused `HistoryFilterTest`/`HistoryViewModelTest`, full `:feature:today:testDebugUnitTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` software launch stayed `emulator-5554 offline` without an online ADB device or `boot_completed=1`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/history-repeated-backlog-filter-emulator/`; cleanup stopped the headless qemu process, removed stale AVD locks, killed ADB, and left no emulator/qemu/adb/netsim process and no AVD lock files.
- The H-11 `dev/history-repeated-backlog-counts` branch on 2026-06-15 adds localized visible same-merchant group counts to History row subtitles while the `Repeated backlog` filter is active. Counts are computed from the visible filtered rows, reuse normalized merchant plus expense/income kind grouping, stay hidden outside repeated-backlog mode, and do not change selection, editing, or exact-rule learning behavior. Focused `HistoryFilterTest`/`HistoryViewModelTest`, full `:feature:today:testDebugUnitTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` software launch stayed `emulator-5554 offline` without an online ADB device or `boot_completed=1`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/history-repeated-backlog-counts-emulator/`; cleanup left no emulator/qemu/adb/netsim process and no AVD lock files.
- The H-12 `dev/history-repeated-backlog-priority` branch on 2026-06-15 sorts the `Repeated backlog` History result by visible same-merchant group size, largest first. The priority order is computed after status/type/source/search filters, keeps rows inside a group stable, keeps equal-size groups deterministic by first visible occurrence, and does not affect `All`, `Uncategorized`, or `Categorized` ordering. Focused `HistoryFilterTest`/`HistoryViewModelTest`, full `:feature:today:testDebugUnitTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` software launch exited before exposing an online ADB device or `boot_completed=1` with exit code `-1073741819`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/history-repeated-backlog-priority-emulator/`; cleanup left no emulator/qemu/adb/netsim process and no AVD lock files.
- The H-13 `dev/history-repeated-backlog-top-group` branch on 2026-06-15 adds a localized `Top group` bulk-selection chip to the `Repeated backlog` History view. The chip selects the largest visible same-merchant group in one action, replaces the current selection, disables once exactly that group is selected, and preserves `Select shown`, `Same merchant`, type-safe category assignment, and exact-rule learning behavior. Focused `HistoryFilterTest`/`HistoryViewModelTest`, full `:feature:today:testDebugUnitTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` software launch stayed `emulator-5554 offline` without an online ADB device or `boot_completed=1`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/history-repeated-backlog-top-group-emulator/`; cleanup left no emulator/qemu/adb/netsim process and no AVD lock files.
- The H-14 `dev/history-bulk-selected-merchant-context` branch on 2026-06-15 adds selected-merchant context to the History bulk selection card and category sheet. The context appears only when selected visible rows share one specific merchant, uses the first visible selected row's merchant label, and stays hidden for mixed selections or generic merchant keys so bulk category decisions are easier to verify before applying. Focused `HistoryFilterTest`/`HistoryViewModelTest`, full `:feature:today:testDebugUnitTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` software launch exited before exposing an online ADB device or `boot_completed=1` with exit code `-1073741819`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/history-bulk-selected-merchant-context-emulator/`; cleanup left no emulator/qemu/adb/netsim process and no AVD lock files.
- The H-15 `dev/history-bulk-category-suggestion` branch on 2026-06-15 makes the History bulk category sheet preselect one active compatible category when selected visible rows share one specific merchant and full local same-merchant history has exactly one category. The sheet shows a localized suggestion/count line, keeps Apply explicit, and hides suggestions for conflicting category history, inactive categories, mixed merchants, or generic merchant keys. Focused `HistoryFilterTest`/`HistoryViewModelTest`, full `:feature:today:testDebugUnitTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` launch through `C:\Users\waok\Android\Sdk\emulator\emulator.exe` exited before exposing an online ADB device or `boot_completed=1` with exit code `-1073741819`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/history-bulk-category-suggestion-emulator/`; cleanup left no new emulator/qemu/netsim process and no AVD lock files.
- The H-16 `dev/history-bulk-suggestion-quick-apply` branch on 2026-06-15 adds a localized explicit `Apply <category>` chip to the History bulk selection card whenever the H-15 conservative category suggestion is valid and normal bulk apply is allowed. It reuses the existing type-safe bulk apply path, keeps the category picker available for review or override, and does not change exact-rule learning, transfer skipping, mixed-type blocking, or suggestion criteria. Focused `HistoryFilterTest`/`HistoryViewModelTest`, full `:feature:today:testDebugUnitTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` launch through `C:\Users\waok\Android\Sdk\emulator\emulator.exe` exited before exposing an online ADB device or `boot_completed=1` with exit code `-1073741819`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/history-bulk-suggestion-quick-apply-emulator/`; cleanup left no new emulator/qemu/netsim process and no AVD lock files.
- The H-17 `dev/history-top-group-suggestion-apply` branch on 2026-06-15 adds a localized explicit `Apply top <category> (<count>)` chip to the `Repeated backlog` History bulk card when the largest visible repeated group has one valid H-15 same-merchant history suggestion. It applies that top group through the existing type-safe bulk apply helper, keeps `Top group`, selected-row quick apply, and category override available, and preserves exact-rule learning plus transfer/mixed-type safeguards. Focused `HistoryFilterTest`/`HistoryViewModelTest`, full `:feature:today:testDebugUnitTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` launch through `C:\Users\waok\Android\Sdk\emulator\emulator.exe` exited before exposing an online ADB device or `boot_completed=1` with exit code `-1073741819`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/history-top-group-suggestion-apply-emulator/`; cleanup left no new emulator/qemu/netsim process and no AVD lock files.
- The H-18 `dev/history-top-group-cleanup-mode` branch on 2026-06-15 keeps History selection mode open after the direct top repeated suggestion action succeeds, clears selected rows, and lets the refreshed repeated-backlog card expose the next largest group without reopening cleanup mode. Normal selected-row bulk apply still exits selection mode, and the direct top action still uses the existing type-safe bulk apply helper, exact-rule learning, transfer skipping, mixed-type safeguards, and result toast. Focused `HistoryFilterTest`/`HistoryViewModelTest`, full `:feature:today:testDebugUnitTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` launch through `C:\Users\waok\Android\Sdk\emulator\emulator.exe` exited before exposing an online ADB device or `boot_completed=1` with exit code `1`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/history-top-group-cleanup-mode-emulator/`; cleanup left no emulator/qemu/netsim process and no AVD lock files.
- The H-19 `dev/history-safe-groups-apply` branch on 2026-06-15 adds a localized explicit `Apply safe groups (<count>)` chip to the `Repeated backlog` History bulk card when more than one visible repeated group has a valid H-15 same-merchant history suggestion. It applies all and only safe suggested groups through the existing type-safe helper, leaves conflicting repeated groups visible and untouched, learns exact local rules for eligible applied groups, keeps selection mode open, and clears selected rows. Focused `HistoryFilterTest`/`HistoryViewModelTest`, full `:feature:today:testDebugUnitTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` launch through `C:\Users\waok\Android\Sdk\emulator\emulator.exe` exited before exposing an online ADB device or `boot_completed=1` with exit code `1`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/history-safe-groups-apply-emulator/`; cleanup left no emulator/qemu/netsim process and no AVD lock files.
- The H-20 `dev/history-safe-groups-summary` branch on 2026-06-15 adds a localized safe-suggestions summary line to the `Repeated backlog` History bulk card whenever the H-19 safe-groups action is available. The line shows both safe repeated groups and total transaction impact before the user applies the explicit action, without changing H-15/H-19 safety criteria, grouping, apply behavior, or exact-rule learning. Focused `HistoryFilterTest`/`HistoryViewModelTest`, full `:feature:today:testDebugUnitTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` software launch exited before exposing an online ADB device or `boot_completed=1` with exit code `-1073741819`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/history-safe-groups-summary-emulator/`; cleanup left no emulator/qemu/netsim process and no AVD lock files.
- The H-21 `dev/history-next-safe-group-apply` branch on 2026-06-15 adds a localized explicit `Apply safe <category> (<count>)` chip to the `Repeated backlog` History bulk card when the largest repeated group has conflicting history but exactly one lower visible repeated group has a valid H-15 same-merchant suggestion. It reuses the H-19 safe-groups apply path for that one safe group, keeps cleanup mode open, clears selected rows, learns the same exact local rule, and leaves the conflicting top group visible and untouched. Focused `HistoryFilterTest`/`HistoryViewModelTest`, full `:feature:today:testDebugUnitTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` software launch exited before exposing an online ADB device or `boot_completed=1` with exit code `-1073741819`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/history-next-safe-group-apply-emulator/`; cleanup left no emulator/qemu/netsim process and no AVD lock files.
- The S-21 `dev/support-diagnostics-category-coverage` branch on 2026-06-15 adds a redacted `category_coverage` block plus per-group and cumulative backlog share to `athar-support-diagnostics.json`. It lets support distinguish concentrated repeated-merchant category cleanup from scattered one-off cleanup while still omitting raw merchants, transaction rows, notes, amounts, balances, senders, and SMS bodies. Focused `SupportDiagnosticsExporterTest`, full `:core:data:testDebugUnitTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, a stale `AtharPixelQaApi35` AVD lock was removed after confirming no emulator/qemu/netsim process was running, and the bounded hidden retry exited before exposing an online ADB device or `boot_completed=1` with exit code `1` because x86_64 emulation currently requires hardware acceleration. Evidence was written to `build/qa/support-diagnostics-category-coverage-emulator/`.
- The R-07 `dev/bulk-categorize-group-impact` branch on 2026-06-15 adds read-only `merchant_group_rank`, `merchant_group_share_permille`, and `merchant_group_cumulative_share_permille` columns to the bulk-categorization CSV so humans and external AI can prioritize the repeated merchant groups with the biggest cleanup payoff. It keeps the importer header-compatible with old/new CSVs and updates the copied prompt plus workflow docs to preserve all non-`category_id` columns. Focused `MerchantBulkCsvTest`/`MerchantBulkAiPromptTest` and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and the bounded hidden `AtharPixelQaApi35` launch exited before exposing an online ADB device or `boot_completed=1` with exit code `1` because x86_64 emulation currently requires hardware acceleration. Evidence was written to `build/qa/bulk-categorize-group-impact-emulator/`; stale AVD locks were cleaned after confirming no emulator/qemu/netsim process was running.
- The R-07 `dev/bulk-categorize-import-preview` branch on 2026-06-15 makes bulk-categorization import safer by previewing the filled CSV before applying it. File selection now runs the same importer in dry-run mode, Settings shows update/rule/skip counts, the selected bytes stay only in ViewModel memory, and transactions/exact local rules write only after **Apply import**. Focused `MerchantBulkCsvTest`/`SettingsViewModelTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and the bounded hidden `AtharPixelQaApi35` launch exited after 5 seconds with exit code `1` because x86_64 emulation currently requires hardware acceleration. Evidence was written to `build/qa/bulk-categorize-import-preview-emulator/`; stale `multiinstance.lock` was cleaned after confirming no emulator/qemu/netsim process was running.
- The R-07 `dev/bulk-categorize-import-impact-summary` branch on 2026-06-15 adds a localized category-impact line to the bulk-categorization dry-run preview. The summary counts the categories that would receive updates after the same transaction matching, category resolution, type checks, and safe same-merchant propagation used by real import, so users can verify category impact before **Apply import** without exposing row ids, merchants, amounts, or raw SMS bodies. Focused `MerchantBulkCsvTest`/`SettingsViewModelTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `AtharPixelQaApi35` exited before exposing an online ADB device with exit code `1` because x86_64 emulation requires hardware acceleration and the Android Emulator hypervisor driver is not installed, so install/screenshot/UI-dump/logcat capture could not run. `AtharPixelQaApi35Arm` exited because arm64 system images are unsupported on this x86_64 host. Evidence was written to `build/qa/bulk-categorize-import-impact-summary-emulator/`; cleanup left no attached ADB device and no emulator/qemu/netsim process.
- The H-22 `dev/today-pending-safe-suggestions` branch on 2026-06-15 adds conservative same-merchant category suggestions directly to Today pending rows. Suggestions are built from confirmed local history only, require one active compatible category for the same specific normalized merchant and expense/income kind, hide conflicts/generic/inactive cases, and the explicit **Apply** action confirms only that row without silently learning a broad rule. Focused `TodayViewModelTest`, the forced gated private SMS audit, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and bounded ADB polling never found an online device, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/today-pending-safe-suggestions-emulator/`; cleanup left no attached ADB device and no emulator/qemu/netsim process.
- The H-23 `dev/today-bulk-safe-suggestions` branch on 2026-06-15 adds a full-width **Apply safe suggestions (N)** action to the Today pending card when H-22 has safe same-merchant category suggestions. The action revalidates every row/category before writing, confirms only rows that remain pending and type-compatible, emits a localized applied-count feedback card, and still avoids broad learned-rule creation. Focused `TodayViewModelTest`, the forced gated private SMS audit, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and `AtharPixelQaApi35` exited before exposing an online ADB device with exit code `1`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/today-bulk-safe-suggestions-emulator/`; final cleanup had no attached ADB device and no emulator/qemu/netsim process.
- The H-24 `dev/today-safe-suggestions-summary` branch on 2026-06-15 adds a category/count impact summary under Today's **Apply safe suggestions (N)** action. The summary is derived from the same H-22 safe suggestion map, groups suggested rows by category, keeps the largest impacts visible, and does not broaden matching, auto-confirm rows, or create learned rules. Focused `TodayViewModelTest`, the forced gated private SMS audit, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. The forced gated private SMS audit wrote only redacted aggregate output: 7,887 records, 3,864 parser successes, 0 parser failures, 0 failed known-bank messages, 2,374 parsed expenses, 1,287 categorized expenses, 1,087 uncategorized expenses, 0 missing-merchant parsed expenses, and `rawBodiesWritten = 0`. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and `AtharPixelQaApi35` exited before exposing an online ADB device with exit code `1`, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/today-safe-suggestions-summary-emulator/`; final cleanup had no attached ADB device and no emulator/qemu/netsim process.
- The G-11 `dev/notification-condo-fee-labels` branch on 2026-06-15 normalizes posted condo, HOA, strata, and building service-charge notification payments to the exact seed-backed `Condo fees` label, preserves named associations/buildings when present, ignores due reminders, assessments, invoices, statements, unpaid, and overdue copy with amounts, and brings active `seed_rules.json` to 704 rules. Focused `GenericBankNotificationTemplateTest`, focused `SeedRulesAssetTest`, the forced gated private SMS audit, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17. The forced gated private SMS audit wrote only redacted aggregate output: 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, 2,374 parsed expenses, 1,287 categorized expenses, 1,087 uncategorized expenses, 0 missing-merchant parsed expenses, 20 hashed uncategorized merchant groups, and `rawBodiesWritten = 0`. Runtime `storeSafeDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, and a bounded hidden `AtharPixelQaApi35` launch never exposed an online ADB device, so install/screenshot/UI-dump/logcat capture could not run. Evidence was written to `build/qa/notification-condo-fee-labels-emulator/`; final cleanup had no emulator/qemu/adb/netsim process and no AVD lock files.
- AVD `AtharPixelQaApi35Arm` is not usable on this x86_64 host because arm64 system images are unsupported here.
- The G-08 `dev/receipt-text-prefill` branch on 2026-06-15 extends Add Transaction quick entry to accept pasted receipt/OCR text. It extracts merchant, total, and four-digit-year receipt dates from conservative English/Arabic receipt-shaped text, skips VAT/tax/subtotal/quantity lines, and reuses matching local recent-merchant categories. Focused `ManualEntryPhraseParserTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `AtharPixelQaApi35` exited before exposing an online ADB device with exit code `1` because x86_64 emulation requires hardware acceleration and the Android Emulator hypervisor driver is not installed, so install/screenshot/UI-dump/logcat capture could not run. `AtharPixelQaApi35Arm` exited because arm64 system images are unsupported on this x86_64 host. Evidence was written to `build/qa/receipt-text-prefill-emulator/`; cleanup left no attached ADB device and no emulator/qemu/netsim process.
- The G-08 `dev/receipt-image-ocr` branch on 2026-06-15 adds on-device ML Kit receipt image OCR after Add Transaction attaches a receipt image. It feeds recognized text through the conservative receipt parser, fills amount/merchant/date/type/category when parseable, keeps Quick Entry text unchanged, preserves an existing same-type category when no local merchant suggestion matches, and reports non-blocking receipt statuses for filled/no-text/failed OCR. Focused `ManualEntryPhraseParserTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `AtharPixelQaApi35` exited before exposing an online ADB device with exit code `1` because x86_64 emulation requires hardware acceleration and the Android Emulator hypervisor driver is not installed, so install/screenshot/UI-dump/logcat capture could not run. `AtharPixelQaApi35Arm` exited with exit code `1` because arm64 system images are unsupported on this x86_64 host. Evidence was written to `build/qa/receipt-image-ocr-emulator/`; final `adb devices -l` listed no attached devices.
- The G-11 `dev/notification-income-labels` branch on 2026-06-15 normalizes posted tax refund, expense reimbursement/refund/reversal/chargeback, and bonus income notification rows to exact seed-backed income labels, preserves specific sources as `Label - source`, ignores tax-refund estimates, reimbursement claims, and bonus promos with amounts, and brings active `seed_rules.json` to 710 rules. Focused `GenericBankNotificationTemplateTest`, focused `SeedRulesAssetTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime install was attempted; `AtharPixelQaApi35` exited before exposing ADB with exit code `1` because x86_64 emulation requires hardware acceleration and the Android Emulator hypervisor driver is not installed, `AtharPixelQaApi35Arm` exited with exit code `1` because arm64 system images are unsupported on this x86_64 host, and final `adb devices` listed no attached devices. Evidence was written to `build/qa/notification-income-labels-emulator/`.
- The G-11 `dev/notification-work-expense-labels` branch on 2026-06-15 normalizes posted work/business expense and office-supplies purchase notification rows to exact seed-backed work-expense labels, preserves named vendors as `Label - vendor`, ignores work-expense claims/reports plus office-supplies quotes, offers, carts, shipping, and order-status copy with amounts, and brings active `seed_rules.json` to 714 rules. Focused `GenericBankNotificationTemplateTest`, focused `SeedRulesAssetTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime install was attempted; `AtharPixelQaApi35` exited before exposing ADB with exit code `1` because x86_64 emulation requires hardware acceleration and the Android Emulator hypervisor driver is not installed, `AtharPixelQaApi35Arm` exited with exit code `1` because arm64 system images are unsupported on this x86_64 host, and final `adb devices` listed no attached devices. Evidence was written to `build/qa/notification-work-expense-labels-emulator/`.
- The G-11 `dev/notification-side-income-labels` branch on 2026-06-15 normalizes posted freelance, rental, dividend, and interest income notification rows to exact seed-backed side-income labels, preserves named sources as `Label - source`, ignores side-income invoices, estimates, projections, rate updates, announcements, and other non-posted side-income copy with amounts, and brings active `seed_rules.json` to 722 rules. Focused `GenericBankNotificationTemplateTest`, focused `SeedRulesAssetTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime install was attempted; `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, `AtharPixelQaApi35` exited with Windows access-violation code `-1073741819` before install, `AtharPixelQaApi35Arm` exited with exit code `1` because arm64 system images are unsupported on this x86_64 host, and final `adb devices` listed no attached devices. Evidence was written to `build/qa/notification-side-income-labels-emulator/`.
- The G-11 `dev/notification-cashback-income-labels` branch on 2026-06-15 normalizes posted cashback credits to exact seed-backed English/Arabic side-income labels, preserves named reward sources as `Cashback income - source`, keeps cashback offers and reward-only copy ignored, and brings active `seed_rules.json` to 724 rules. Focused `GenericBankNotificationTemplateTest`, focused `SeedRulesAssetTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime install was attempted; `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, `AtharPixelQaApi35` exited before install with exit code `1` because x86_64 emulation requires hardware acceleration, `AtharPixelQaApi35Arm` exited with exit code `1` because arm64 system images are unsupported on this x86_64 host, and final `adb devices` listed no attached devices. Evidence was written to `build/qa/notification-cashback-income-labels-emulator/`.
- The G-11 `dev/notification-deposit-income-labels` branch on 2026-06-15 normalizes posted cash, ATM, check, and cheque deposit notifications to exact seed-backed `Other income` labels, preserves check-deposit sources as `Label - source`, ignores deposit limits, holds, pending availability, fees, rates, and other non-posted deposit copy with amounts, and brings active `seed_rules.json` to 729 rules. Focused `GenericBankNotificationTemplateTest`, focused `SeedRulesAssetTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime install was attempted; `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, `AtharPixelQaApi35` exited before install with exit code `1` because x86_64 emulation requires hardware acceleration, `AtharPixelQaApi35Arm` exited with exit code `1` because arm64 system images are unsupported on this x86_64 host, and final `adb devices` listed no attached devices. Evidence was written to `build/qa/notification-deposit-income-labels-emulator/`.
- The S-21 `dev/support-diagnostics-backlog-recommendations` branch on 2026-06-15 adds a redacted `category_backlog_recommendation` block to `athar-support-diagnostics.json`, deriving aggregate action codes from backlog size and hashed repeated-merchant group sizes only. It lets support recommend History repeated-backlog cleanup, bulk CSV categorization, small manual cleanup, or no category action without exposing raw merchants, amounts, notes, SMS bodies, or transaction rows. Focused `SupportDiagnosticsExporterTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, `AtharPixelQaApi35` exited before install with exit code `1` because x86_64 emulation requires hardware acceleration, `AtharPixelQaApi35Arm` exited with exit code `1` because arm64 system images are unsupported on this x86_64 host, and final `adb devices` listed no attached devices. Evidence was written to `build/qa/support-diagnostics-backlog-recommendations-emulator/`.
- The R-07 `dev/bulk-categorize-specific-merchant-guards` branch on 2026-06-15 makes the bulk categorization CSV and importer treat generic merchant labels such as `payment`, `purchase`, `cash`, `merchant`, and `bank` as row-by-row work only. Specific repeated merchants still get group counts/ranks, representative-row inheritance, and exact learned rules; generic explicit rows still update when valid, but generic keys no longer propagate categories to blank peers or train local exact rules. Focused `MerchantBulkCsvTest`, focused `MerchantBulkAiPromptTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, `AtharPixelQaApi35` exited before install with exit code `1` because x86_64 emulation requires hardware acceleration, `AtharPixelQaApi35Arm` exited with exit code `1` because arm64 system images are unsupported on this x86_64 host, and final `adb devices` listed no attached devices. Evidence was written to `build/qa/bulk-categorize-specific-merchant-guards-emulator/`.
- The R-07 `dev/shared-specific-merchant-guard` branch on 2026-06-15 centralizes the specific-merchant guard in `core:domain` and shares it across bulk categorization, Today pending suggestions, History repeated-backlog cleanup, and local auto-learning. Specific merchants still get safe same-merchant shortcuts, while generic English/Arabic labels such as `payment`, `purchase`, `cash`, `merchant`, `bank`, `كاش`, and `دفع` remain explicit row-by-row work that cannot produce suggestions, repeated cleanup groups, inherited bulk rows, or exact local rules. Focused `MerchantSpecificityTest`, `MerchantBulkCsvTest`, `LocalCategoryRuleLearnerTest`, `TodayViewModelTest`, `HistoryFilterTest`, and `HistoryViewModelTest` passed; `git diff --check` passed; the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, `AtharPixelQaApi35` exited after 5 seconds with exit code `1` because x86_64 emulation requires hardware acceleration, `AtharPixelQaApi35Arm` exited after 5 seconds with exit code `1` because arm64 system images are unsupported on this x86_64 host, and final `adb devices` listed no attached devices. Evidence was written to `build/qa/shared-specific-merchant-guard-emulator/`.
- The S-21 `dev/support-diagnostics-specific-merchant-guard` branch on 2026-06-15 makes support diagnostics reuse the shared specific-merchant guard for hashed uncategorized merchant groups and category-backlog recommendations. Generic labels such as `payment`, `purchase`, `cash`, `merchant`, `bank`, `كاش`, and `دفع` still count toward total category backlog but no longer appear as repeated-group evidence or trigger `history_repeated_backlog`; those rows remain in other/manual or bulk-CSV cleanup. Focused `SupportDiagnosticsExporterTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, `AtharPixelQaApi35` exited after 5 seconds with exit code `1` because x86_64 emulation requires hardware acceleration, `AtharPixelQaApi35Arm` exited after 5 seconds with exit code `1` because arm64 system images are unsupported on this x86_64 host, and final `adb devices` listed no attached devices. Evidence was written to `build/qa/support-diagnostics-specific-merchant-guard-emulator/`.
- The G-03 `dev/recurring-suggestions-specific-merchant-guard` branch on 2026-06-15 makes auto-detected recurring suggestions reuse the shared specific-merchant guard before grouping history. Specific repeated merchants still produce suggestions, while generic labels such as `payment`, `purchase`, `cash`, `merchant`, `bank`, `كاش`, and `دفع` cannot become subscription/rule suggestions. Focused `RecurringSuggestionRepositoryImplTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, `AtharPixelQaApi35` exited after 5 seconds with exit code `1` because x86_64 emulation requires hardware acceleration, `AtharPixelQaApi35Arm` exited after 5 seconds with exit code `1` because arm64 system images are unsupported on this x86_64 host, and final `adb devices` listed no attached devices. Evidence was written to `build/qa/recurring-suggestions-specific-merchant-guard-emulator/`.
- The G-08 `dev/manual-entry-specific-merchant-suggestions` branch on 2026-06-15 makes Add Transaction quick-add/autocomplete chips reuse the shared specific-merchant guard. Specific confirmed merchants still produce local chips, blank normalized merchants can fall back to a specific display merchant, and generic labels such as `payment`, `purchase`, `cash`, `merchant`, `bank`, `كاش`, and `دفع` no longer appear as routine manual-entry shortcuts. Focused `ManualEntrySuggestionBuilderTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, `AtharPixelQaApi35` exited after 5 seconds with exit code `1` because x86_64 emulation requires hardware acceleration, `AtharPixelQaApi35Arm` exited after 5 seconds with exit code `1` because arm64 system images are unsupported on this x86_64 host, and final `adb devices` listed no attached devices. Evidence was written to `build/qa/manual-entry-specific-merchant-suggestions-emulator/`.
- The H-03 `dev/edit-rule-learning-specific-merchant-guard` branch on 2026-06-15 makes Today/History edit-sheet "Always categorize" learning use the shared specific-merchant guard. Specific merchants still prompt, learn, and backfill matching pending/dismissed rows; generic labels such as `payment`, `purchase`, `cash`, `merchant`, `bank`, `كاش`, and `دفع` save as one-row category edits without prompting or creating broad substring rules/backfills. Focused `TodayViewModelTest`/`HistoryViewModelTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, `AtharPixelQaApi35` exited after 5 seconds with exit code `1` because x86_64 emulation requires hardware acceleration, `AtharPixelQaApi35Arm` exited after 5 seconds with exit code `1` because arm64 system images are unsupported on this x86_64 host, and final `adb devices` listed no attached devices. Evidence was written to `build/qa/edit-rule-learning-specific-merchant-guard-emulator/`.
- The C-01 `dev/community-rules-specific-merchant-guard` branch on 2026-06-15 makes the community-rule exporter reuse the shared specific-merchant guard before writing JSON. Specific explicit substring rules still export for maintainer review, while legacy learned substring rules for generic labels such as `payment`, `purchase`, `cash`, `merchant`, `bank`, `كاش`, and `دفع` stay local along with exact import/repeated-history rules. Focused `CommunityRulesShareExporterTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, `AtharPixelQaApi35` exited before install with exit code `1` because x86_64 emulation requires hardware acceleration, `AtharPixelQaApi35Arm` exited with exit code `1` because arm64 system images are unsupported on this x86_64 host, and final `adb devices` listed no attached devices. Evidence was written to `build/qa/community-rules-specific-merchant-guard-emulator/`.
- The R-05 `dev/seed-rules-specific-merchant-pattern-types` branch on 2026-06-15 lets bundled `seed_rules.json` rows opt into exact matching through `patternType`. Existing merchant seeds still default to substring, while generic-prefix parser-label seeds such as `cash deposit`, `bank fees`, and `كاش باك بطاقة ائتمانية` now load as exact rules. `SeedRulesAssetTest` pins the exact rows and rejects future generic-prefix substring seeds; `scripts/merge_ai_seed_rules.py` preserves existing `patternType` values. Focused `SeedRulesAssetTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime `personalFullSmsDebug` install was attempted; the built APK was ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, `AtharPixelQaApi35` exited before install with exit code `1` because x86_64 emulation requires hardware acceleration, `AtharPixelQaApi35Arm` exited with exit code `1` because arm64 system images are unsupported on this x86_64 host, and final `adb devices` listed no attached devices. Evidence was written to `build/qa/seed-rules-specific-merchant-pattern-types-emulator/`.
- The R-05 `dev/seed-rules-content-refresh` branch on 2026-06-15 makes bundled seed refresh content-aware instead of count-only. `RuleSeed` now compares bundled signatures (`pattern`, `patternType`, `categoryId`, `priority`) before skipping startup seeding, so existing installs receive metadata-only seed updates such as exact `patternType` changes while user-learned and `auto-local-*` exact rules remain local and outside the refresh/delete path. Focused `RuleSeedRefreshTest`/`SeedRulesAssetTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime install was attempted; the built `personalFullSmsDebug` and `storeSafeDebug` APKs were ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, `AtharPixelQaApi35` exited with exit code `1` because x86_64 emulation requires hardware acceleration, `AtharPixelQaApi35Arm` exited with exit code `1` because arm64 system images are unsupported on this x86_64 host, and final `adb devices` listed no attached devices. Evidence was written to `build/qa/seed-rules-content-refresh-emulator/`; cleanup left no emulator/qemu/adb/netsim process and no AVD lock files.
- The R-07 `dev/bulk-ai-prompt-label-guidance` branch on 2026-06-15 extends the copied external-AI bulk categorization prompt with seed-backed label-to-category guidance for current normalized parser labels, including ATM withdrawals, bank fees, debt/housing/admin labels, tax refunds, reimbursements, cashback, deposits, side income, work expenses, life-admin, shopping, travel, and event tickets. This keeps the no-API CSV workflow but gives ChatGPT/Claude better category cues, especially for private exports where `raw_body` is blank. Focused `MerchantBulkAiPromptTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime install was attempted; the built `personalFullSmsDebug` and `storeSafeDebug` APKs were ready, `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, `AtharPixelQaApi35` exited with exit code `1` because x86_64 emulation requires hardware acceleration, `AtharPixelQaApi35Arm` exited with exit code `1` because arm64 system images are unsupported on this x86_64 host, and final `adb devices` listed no attached devices. Evidence was written to `build/qa/bulk-ai-prompt-label-guidance-emulator/`; cleanup left no emulator/qemu/adb/netsim process and no AVD lock files.
- The R-18 `dev/private-audit-backlog-recommendation` branch on 2026-06-15 adds a redacted `categoryBacklogRecommendation` block to the gated private-corpus audit, mirroring the support diagnostics action signal without serializing raw SMS bodies, merchants, amounts, senders, notes, or transaction rows. Focused `PrivateSmsExportAuditTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. The forced root report wrote 7,887 records, 3,864 parser successes, 4,023 ignored, 0 parser failures, 0 failed known-bank messages, 2,374 parsed expenses, 1,287 categorized expenses, 1,087 uncategorized expenses, 0 missing-merchant parsed expenses, 20 hashed uncategorized groups, `categoryBacklogRecommendation.primaryAction = history_repeated_backlog`, recommended actions `history_repeated_backlog` then `bulk_categorize_export`, and `rawBodiesWritten = 0`. Runtime install was attempted; the built `personalFullSmsDebug` APK was ready, but `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, the x86 `AtharPixelQaApi35` software launch exposed only `emulator-5554 offline`, `AtharPixelQaApi35Arm` exited because arm64 system images are unsupported on this x86_64 host, and no online ADB device appeared. Evidence was written to `build/qa/private-audit-backlog-recommendation-emulator/`; cleanup stopped the leftover qemu process, removed stale AVD locks, and left no emulator/qemu/adb/netsim process.
- The H-25 `dev/today-repeated-backlog-nudge` branch on 2026-06-15 makes the private-audit/support recommendation actionable from Today. Today now shows an aggregate-only nudge when repeated specific uncategorized expense/income groups exist, ignores generic merchant labels, and opens History with `Repeated backlog` selected plus selection mode active so the existing top-group/safe-group cleanup controls are visible immediately. Focused `TodayViewModelTest`/`HistoryFilterTest`, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime install was attempted; the built `personalFullSmsDebug` APK was ready, but `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, the x86 `AtharPixelQaApi35` launch exited before exposing an online ADB device with code `-1073741819`, `AtharPixelQaApi35Arm` exited because arm64 system images are unsupported on this x86_64 host, and final `adb devices` listed no attached devices. Evidence was written to `build/qa/today-repeated-backlog-nudge-emulator/`; cleanup removed stale AVD locks and left no emulator/qemu/netsim/adb process.
- The W-10 `dev/wishlist-reconciliation-capacity` branch on 2026-06-15 makes Plan -> Wishlist monthly capacity use confirmed operating transactions only, excluding balance reconciliation adjustments through `Transaction.isReconciliation()` before summing income and expenses. This keeps manual balance corrections visible in net worth and account history without turning them into fake Wishlist surplus or pressure. Focused `WishlistCalcTest`, Plan feature compilation, `git diff --check`, and the full JVM/debug-APK/lint stack passed with JDK 17 and `ATHAR_PRIVATE_SMS_EXPORT` set. Runtime install was attempted; the built `personalFullSmsDebug` APK was ready, but `emulator -accel-check` reported the Android Emulator hypervisor driver is not installed, the x86 `AtharPixelQaApi35` launch stayed without an ADB transport during the bounded window and opened emulator crash-consent logging, `AtharPixelQaApi35Arm` exited because arm64 system images are unsupported on this x86_64 host, and final `adb devices` listed no attached devices. Evidence was written to `build/qa/wishlist-reconciliation-capacity-emulator/`; cleanup stopped the leftover qemu process, removed stale AVD locks, and left no attached ADB device.

Before calling the integrated stack release-ready, run the app on a physical Android device with USB debugging or enable virtualization / install the Android Emulator hypervisor driver, then run:

- Install and smoke-test `personalFullSmsDebug`.
- Install and smoke-test `storeSafeDebug`.
- Exercise onboarding CSV import, manual add quick-add, Settings exports, Plan Bills/Goals/Wishlist, History source filters, SMS audit sender health, rescan confirmation, widgets, and recurring auto-run.
- Capture screenshots/logcat for any UI regression or crash.
