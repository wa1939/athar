# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
gradle-app.setting
.gradletasknamecache
local.properties

# Android Studio / IntelliJ
.idea/
*.iml
*.ipr
*.iws
captures/
out/

# Build outputs
*.apk
*.aab
*.ap_
*.dex
*.class
bin/
gen/
obj/
release/

# Keys & secrets — NEVER commit
*.jks
*.keystore
keystore.properties
signing.properties
*.env
google-services.json

# Generated / KSP / Compose
.kotlin/
generated/
**/build/**

# Reports
**/reports/
**/paparazzi/failures/

# OS
.DS_Store
Thumbs.db
__MACOSX/

# User backups (never source-controlled)
*.athar
*.athar.bak

# Personal seed data — only the owner's local build sees this directory.
# The data is exported from `Budget Tracking Tool - TMOAP v5.xlsx` by
# `scripts/extract_tmoap.py` and packaged into the seeded APK at build time.
/app/src/seeded/

# Logs
*.log
hs_err_pid*

# Personal data & third-party copyrighted material the public repo shouldn't ship
# These stay in the local working directory only.
/Budget Tracking Tool - TMOAP v5.xlsx
/qayd_android_budget_app_prd.md
/دليل جماليات خط ثمانية.pdf
/authar logo.png
/authar logo no background.png
/thmanyah typeface/

# Built APKs — kept locally for sideload, never published to git.
# GitHub Releases is the canonical distribution surface for public APKs.
/release-artifacts/
/error.jpg
/error*.jpg

# E2E test artifacts (ad-hoc emulator dumps — never PR-able)
/test-screens/
/t[0-9]*.png
/qa_*.png
/g5*.png
/scripts/test_sms.sh
/scripts/_sms_insert.sql

# Claude Code session exports — these can contain conversation transcripts with
# user PII or credentials. Saved locally only.
/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]-*.txt

# PII: this folder contains the user's own SMS exports and their friend's,
# plus the full AI triage outputs (which embed raw SMS bodies). Keep local only.
"/AI template and output Categorization/"
/AI template and output Categorization/

# Personal SMS exports — these are PII and must never be committed.
# The folder is used as input for `docs/sms-corpus-analysis.md` and the
# merchant catalog, both of which are derived (no personal data leaks).
"/messages sample/"
/messages sample/

# .claude scratch dir (private session state)
.claude/

# Memory/plan files from local agent tools
memory/

# GitHub Actions workflows — temporarily ignored on initial push because
# the local gh OAuth token doesn't carry the `workflow` scope. Once you've run
# `gh auth refresh -s workflow`, remove this block and commit the workflows.
/.github/workflows/
